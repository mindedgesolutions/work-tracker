import React from "react";

const UserDashboard = () => {
  document.title = `User Dashboard | ${import.meta.env.VITE_USER_TITLE}`;

  return <div>UserDashboard</div>;
};

export default UserDashboard;
